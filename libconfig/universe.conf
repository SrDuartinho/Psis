# Universe simulation configuration
# Values are clamped to compile-time maxima; lower them freely. 
# planet_num: max planets to create (<= PLANET_NUM built in)
# trash_amount: initial trash pieces spawned
# window_size: window width/height in pixels
# ship_capacity: per-ship trash capacity
# max_trash_world: global trash cap (<= MAX_TRASH_WORLD built in)

planet_num = 15;
trash_amount = 10;
window_size = 1000;
ship_capacity = 5;
max_trash_world = 20;
